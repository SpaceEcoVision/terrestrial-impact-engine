# GPU workload dependencies (kept separate from the lightweight engine core)
torch>=2.2
numpy>=1.24
# prepare_data.py only: read ESA WorldCover label COGs + reproject to the bbox
rasterio>=1.3
python-dotenv>=1.0
sentinelhub>=3.9
